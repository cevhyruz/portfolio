import { // {{{1
  cilSun,
  cilMoon,
  cilContrast,
  cilCheckAlt,
} from '@coreui/icons';

export const iconSubset = { // {{{1
  cilSun,
  cilMoon,
  cilContrast,
  cilCheckAlt,
}

export enum IconSubset { // {{{1
  cilSun = 'cilSun',
  cilMoon = 'cilMoon',
  cilContrast = 'cilContrast',
  cilCheckAlt = 'cilCheckAlt',
}
